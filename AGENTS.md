# Idea
The idea of this app is to combine messaging and also file storage, because for most people, they just create an empty group with only one member and send all their documents here. This app is entirely for that purpose.

1. Run `flutter analyze` after major edits
